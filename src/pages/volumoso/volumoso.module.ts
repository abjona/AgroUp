import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { VolumosoPage } from './volumoso';

@NgModule({
  declarations: [
    VolumosoPage,
  ],
  imports: [
    IonicPageModule.forChild(VolumosoPage),
  ],
})
export class VolumosoPageModule {}
